1. Can't have spaces after entries in settings.py. Puts this space into the field.
2. Does hg update have user input? Yes sometimes, uhhh ( hg pull --update --force )?
3. No need to parse settings.py if it is unchanged and no syncdb is needed.
3. Upgrade charm can be created.
